# SyncClient

:::{deprecated} 0.9
`SyncClient` is a low-level connection primitive, kept importable for one
deprecation cycle. Connect with the synchronous
[`ClusterDefinition`](cluster-definition.md) and operate through
[`SyncSession`](session.md) / [`Cluster`](cluster.md) instead.
:::

```{eval-rst}
.. autoclass:: aerospike_sdk.sync.client.SyncClient
   :members:
   :show-inheritance:
```
